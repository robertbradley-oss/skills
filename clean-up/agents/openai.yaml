interface:
  display_name: "Clean Up"
  short_description: "Finish tasks and safely clean workspaces"
  default_prompt: "Use $clean-up to finish the current implementation task with scoped simplification, organization, cleanup, and validation, completing authorized edits and requesting only any exact removal approval still needed."
