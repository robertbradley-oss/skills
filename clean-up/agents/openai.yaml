interface:
  display_name: "Clean Up"
  short_description: "Prove and safely remove cleanup candidates"
  default_prompt: "Use $clean-up to discover possible cleanup, investigate release-retention evidence, and remove nothing without my exact approval."
