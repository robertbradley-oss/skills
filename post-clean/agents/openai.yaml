interface:
  display_name: "Post Clean"
  short_description: "Safely inspect and remove repository residue"
  default_prompt: "Use $post-clean to inspect the exact paths I name using current Git evidence, then apply only candidate IDs I explicitly approve."
