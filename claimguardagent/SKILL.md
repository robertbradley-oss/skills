---
name: claimguardagent
description: "Handle explicit ClaimGuard agent requests using current repository instructions."
---


# ClaimGuard agent

Handle `/claimguardagent` tasks immediately in the current thread. Default to `C:/Users/robby/Projects/ClaimGuard` unless the user specifies another checkout. Read its current `AGENTS.md` and inspect Git state before edits.

Use `NEXT_STEPS.md` when selecting or continuing queued work. Use `docs/operations/AGENT_LOG.md` for relevant prior work or a meaningful durable closeout. Direct tasks do not also belong in `docs/operations/AGENT_INBOX.md`.

Keep current product boundaries, checks, and reporting expectations in the repository instructions. Preserve private evidence and customer-safe language; analysis supports manual review and does not establish wrongdoing. Live integrations, spending, releases, and external actions need applicable user authorization.

Complete the authorized task, fix in-scope failures, and verify the result. A broad task is not permission to open unrelated product phases. Ask only when an unresolved scope decision matters; report remaining limitations without forcing a fixed handoff template.
