interface:
  display_name: "Clean Handoff"
  short_description: "Preserve context for reliable task handoffs"
  default_prompt: "Use $clean-handoff to preserve the evidence, decisions, source links, and next action needed to continue this work, including whether to start now or wait."
policy:
  allow_implicit_invocation: true
