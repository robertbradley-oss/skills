interface:
  display_name: "Clean Handoff"
  short_description: "Fast handoff to a new task or copyable text"
  default_prompt: "Use $clean-handoff to hand this work off with only the context needed to continue."
policy:
  allow_implicit_invocation: true
